Dependency Injection - Implementation technique for populating instance variables
of a class. Object (client) recieves other objects that it depends on (service). 
The client is never responsible for creating or supplying their own dependencies. 

Dependency Inversion - General design guideline which recommends that classes
should have have direct relationships with high-level abstractions

